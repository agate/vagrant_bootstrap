site :opscode

# apt 1.8.2+ requires Chef 10.16.4 or later, but precise64's chef is 10.14.2
# if you want to upgrade box's chef version you can rebuild a new box
cookbook 'apt', '1.7.0'
cookbook 'ubuntu'
cookbook 'rvm', github: 'fnichol/chef-rvm'
